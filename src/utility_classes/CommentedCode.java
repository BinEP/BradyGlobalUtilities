package utility_classes;

public class CommentedCode {

	public CommentedCode() {
		// TODO Auto-generated constructor stub
	}

	/*
	 * 
	 * Control scale() 
	 * 
	 * boolean widthLarger = getWidth() >= getHeight();
	 * 
	 * 
	 * 
	 * g.scale(scale, scale);
	 * 
	 * System.out.println("Windows Width: " + Windows.WIDTH);
	 * System.out.println("Windows Height: " + Windows.HEIGHT); if (getWidth()
	 * != Windows.WIDTH) { System.out.println("Actual Width: " + getWidth());
	 * System.out.println("Actual Height: " + getHeight());
	 * System.out.println(trans); } double currentTransY =
	 * trans.getTranslateY(); trans.setToScale(scale, scale);
	 * 
	 * int diffHeight = (int) ((getHeight() - Windows.HEIGHT * scale) / 2); if
	 * (getWidth() != Windows.WIDTH) {
	 * 
	 * System.out.println("Scale: " + scale); System.out.println("Diff Height: "
	 * + diffHeight); System.out.println("Current Trans Y: " + currentTransY);
	 * System.out.println(); } trans.translate(0, diffHeight - currentTransY);
	 * 
	 * g.transform(trans);
	 * 
	 * 
	 * 
	 * Previous Timer keeping
	 * 
	 * startTime
	 * startTime = System.currentTimeMillis();
	 * 
	 * stopTime
	 * totalTime += System.currentTimeMillis() - startTime;
	 * startTime = System.currentTimeMillis();
	 * 
	 * getTime
	 * if (!paused) stopTime();
	 * return (int) (totalTime / 1000);
	 * 
	 * resetTime
	 * totalTime = 0;
	 * startTime = System.currentTimeMillis();
	 * 
	 * 
	 * actionPerformed
	 * 
	 * setting width stuff
	 * 
	 * width = getWidth();
	 * height = getHeight();
	 * System.out.println("width: " + width + "\t height: " + height);
	 * 
	 */

}
